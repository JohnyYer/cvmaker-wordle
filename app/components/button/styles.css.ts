import { style } from "@vanilla-extract/css";

export const root = style({
  color: "white",
  padding: 6,
  minWidth: 228,
  borderRadius: 5,
  background:
    "linear-gradient(0deg, #007AFF, #007AFF), linear-gradient(180deg, rgba(255, 255, 255, 0.17) 0%, rgba(255, 255, 255, 0) 100%)",
});
